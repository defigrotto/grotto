{
  "name": "grotto-contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test", 
    "test:sokol": "npx hardhat test --network sokol", 
    "compile": "npx hardhat compile",
    "start-node": "npx hardhat node",
    "deploy": "npx hardhat run scripts/deploy.js --network sokol",
    "console": "npx hardhat console",
    "deploy:gov": "npx hardhat run scripts/deploy_gov.js --network sokol",
    "deploy:store": "npx hardhat run scripts/deploy_store.js --network sokol",
    "deploy:grotto_token": "npx hardhat run scripts/deploy_grotto_token.js --network sokol",
    "deploy:all": "npm run deploy:grotto_token && npm run deploy:store &&  npm run deploy:gov &&  npm run deploy",
    "flatten": "./node_modules/.bin/poa-solidity-flattener",
    "index": "npx hardhat run --network localhost scripts/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.1",
    "@openzeppelin/contracts": "^3.4.0",
    "@openzeppelin/test-helpers": "^0.5.10",
    "@poanet/solidity-flattener": "^3.0.6",
    "chai": "^4.3.0",
    "ethers": "^5.0.29",
    "hardhat": "^2.0.8",
    "npx": "^10.2.2",
    "truffle": "^5.1.65"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.1.6"
  }
}
